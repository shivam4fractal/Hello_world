with enriched as (
  select * from {{ ref(int_inet_loads_enriched) }}
)
select
  enriched.internal_order_id_nk,
  enriched.internal_load_id_nk,
  enriched.costid_nk,
  enriched.internal_orderdetail_id_nk,
  enriched.to_bonus_time,
  enriched.to_order_number,
  enriched.to_load_number,
  enriched.ld_load_name,
  enriched.means_of_transport,
  enriched.to_special_order_type,
  enriched.to_act_pickup_date,
  enriched.co_total_costs,
  enriched.co_cost_type,
  enriched.to_weight_unit,
  enriched.to_gross_weight,
  enriched.packagetype,
  enriched.to_consigner_city,
  enriched.to_consigner_country,
  enriched.lane_name,
  enriched.route_distance,
  enriched.to_purchase_order_number,
  enriched.to_consignor_id,
  enriched.totalpalletsactual,
  enriched.totalpalletsplaned,
  enriched.to_status,
  enriched.to_floor_pallets_actuals,
  enriched.to_floor_pallets_planned,
  enriched.pickup_status,
  enriched.to_reference_number,
  enriched.to_delivery_note_number,
  enriched.to_recipient_city,
  enriched.to_recipient_country,
  enriched.to_recipient_id,
  enriched.to_recipient_name,
  enriched.to_recipient_zipcode,
  enriched.to_remarks,
  enriched.transport_start_date,
  enriched.to_consigner_name,
  enriched.to_pallet_places,
  enriched.ld_transport_end_date
from enriched